#pragma once
// CastEntry is superseded by ActorDocument.  This header is kept so that
// any translation unit that still includes it continues to compile.
#include "ActorDocument.h"
using CastEntry = ActorDocument;
