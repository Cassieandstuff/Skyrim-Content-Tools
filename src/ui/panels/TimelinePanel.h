#pragma once

// Phase 1: empty stub panel.
// Phase 7: custom-drawn timeline — per-actor track groups, clip blocks,
//          ARKit face mocap track, camera track, dialogue track, playhead.
struct TimelinePanel {
    static void Draw();
};
